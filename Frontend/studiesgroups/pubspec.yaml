name: studiesgroups
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  url_launcher: ^5.5.0
  http: ^0.12.2
  connectivity: ^0.4.9+2

flutter:
  uses-material-design: true
